# Linux Mastery Project
This script automates file checking in a shared environment.

## Requirements
- Directory `/shared_data` must be set to `chmod 745`.
- Environment variable `DATA_PATH` must be defined.

## Usage
export DATA_PATH="/shared_data"
./check_logic.sh
